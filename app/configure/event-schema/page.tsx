import PlaceholderPage from '../PlaceholderPage';

export default function EventSchemaPage() {
  return (
    <PlaceholderPage
      title="Event Schema"
      description="Event schema configuration will be available here."
    />
  );
}
